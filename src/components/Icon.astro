---
interface Props {
  iconName: string;
  width?: number;
  backgroundColor?: string;
  alt?: string;
}

const { iconName, width, backgroundColor, alt } = Astro.props;
---

<img src={`${import.meta.env.BASE_URL}icons/${iconName}.svg`} width={width} alt={alt} style={{fill: backgroundColor}} />
